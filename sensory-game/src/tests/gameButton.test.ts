import { render } from "@testing-library/react";
import GameButton, {
  GameButtonProps,
} from "../components/game-button/gameButton";
import React from "react";

describe.only("GameButton", () => {
  it("should return sound when clicked", () => {
    console.log("in the test");
  });
});
